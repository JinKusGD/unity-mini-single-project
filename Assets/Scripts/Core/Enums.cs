public enum InputCallbackType
{
    None = 0,
    Started,
    Performed,
    Canceled
}

public enum InputActionType
{
    PlayerMove,
    PlayerDash
}

public enum UIRoot
{
    Main,
    Content,
    Popup,
    System,
    TopMost
}

public enum UIType
{
    Debug
}